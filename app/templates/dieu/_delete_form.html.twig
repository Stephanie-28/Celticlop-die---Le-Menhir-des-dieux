<form method="post" action="{{ path('app_dieu_delete', {'id': dieu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dieu.id) }}">
    <button class="btn">Delete</button>
</form>
