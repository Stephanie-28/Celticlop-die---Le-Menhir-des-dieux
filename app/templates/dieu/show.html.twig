{% extends 'base.html.twig' %}

{% block title %}Dieu{% endblock %}

{% block body %}
    <h1>Dieu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dieu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dieu.name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ dieu.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dieu.description }}</td>
            </tr>
            <tr>
                <th>Quote</th>
                <td>{{ dieu.quote }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ dieu.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dieu_index') }}">back to list</a>

    <a href="{{ path('app_dieu_edit', {'id': dieu.id}) }}">edit</a>

    {{ include('dieu/_delete_form.html.twig') }}
{% endblock %}
